[package]
name = "temporal-fuzz"
version = "0.1.0"
edition = "2021"
description = "Temporal differential fuzzing for streaming parser boundary confusion"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

